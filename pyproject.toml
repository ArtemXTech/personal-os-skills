[project]
name = "personal-os-skills"
version = "0.1.0"
description = "Python utilities for Personal OS Obsidian skills"
requires-python = ">=3.11"
dependencies = [
  "networkx>=3.5",
  "pyvis>=0.3.2",
]

[tool.uv]
package = false
